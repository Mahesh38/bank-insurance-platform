# =============================================================================
# SANDBOX / DEMO ONLY — agent & local connectivity validation
#
# Authorised under SUG-20260913-osk (human ADMIT-BYPASS of secrets-in-git).
# NEVER use these values for uat or prod. NEVER copy into application-prod.yml.
# Rotate via 1SB RM if this repo becomes public or these keys are elevated.
#
# Usage (from repo root):
#   set -a && source config/onesb/sandbox-agent.credentials.env && set +a
#   # then bootRun / curl against https://demo.api.1silverbullet.tech
#
# Spring PROPERTIES source also binds these via relaxed env → onesb.api-* keys.
# Prefer the sibling .properties file with:
#   --spring.config.additional-location=optional:file:./config/onesb/sandbox-agent.credentials.properties
# =============================================================================

ONESB_API_KEY=finoux_key
ONESB_API_SECRET=finoux_secret
ONESB_DISTRIBUTOR_ID=BCIBL
