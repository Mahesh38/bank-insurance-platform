# Icon assets only — the official AWS, Kubernetes, Flutter and Argo art ships
# inside this wheel. No layout engine is used from it; see svgcanvas.py.
diagrams==0.25.1
# PNG companions for the SVGs. Optional: without it the build emits SVG only.
cairosvg==2.7.1
