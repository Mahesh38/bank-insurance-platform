# {{PROJECT_NAME}} Context

This folder is a project instance of the portable context framework.

Start with [problem-statement.md](./problem-statement.md), then validate
`context-manifest.yaml`. Context is descriptive and never overrides the authority sources listed
in the manifest.
