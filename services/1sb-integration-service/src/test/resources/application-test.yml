insurance:
  secrets:
    source: PROPERTIES

bank:
  persistence:
    base-url: http://localhost:8081

# Stub credentials for tests — NOT real secrets
onesb:
  api-key: test-api-key
  api-secret: test-api-secret
  distributor-id: TEST_DIST
  client:
    base-url: http://localhost:9
    connect-timeout-ms: 3000
    read-timeout-ms: 30000

logging:
  level:
    com.bank.insurance: WARN

# Enabled for tests only: OpenApiContractTest reads /v3/api-docs to verify the committed
# specification still matches the code. Production defaults stay off (CR-002).
springdoc:
  api-docs:
    enabled: true
  swagger-ui:
    enabled: false
