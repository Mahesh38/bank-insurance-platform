/**
 * Domain model, invariants and outbound ports for bounded context #19 — Administration & Config.
 *
 * <p>Domain stays pure Java — no Spring, Jackson or Jakarta persistence annotations.
 */
package com.bank.platform.adminconfig.domain;
