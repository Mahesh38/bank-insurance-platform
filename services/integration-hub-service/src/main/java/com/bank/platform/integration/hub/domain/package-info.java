/**
 * Domain model, invariants and outbound ports for bounded context #14 — Integration Hub.
 *
 * <p>Domain stays pure Java — no Spring, Jackson or Jakarta persistence annotations.
 */
package com.bank.platform.integration.hub.domain;
