/**
 * Domain model, invariants and outbound ports for bounded context #9 — Journey Orchestration.
 *
 * <p>Domain stays pure Java — no Spring, Jackson or Jakarta persistence annotations.
 */
package com.bank.platform.journey.domain;
