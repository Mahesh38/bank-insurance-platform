/**
 * Domain model, invariants and outbound ports for bounded context #5 — Lead.
 *
 * <p>Domain stays pure Java — no Spring, Jackson or Jakarta persistence annotations.
 */
package com.bank.platform.lead.domain;
