/**
 * Domain model, invariants and outbound ports for bounded context #17 — Notification.
 *
 * <p>Domain stays pure Java — no Spring, Jackson or Jakarta persistence annotations.
 */
package com.bank.platform.notification.domain;
