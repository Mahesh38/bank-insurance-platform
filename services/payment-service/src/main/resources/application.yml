server:
  port: 8098

spring:
  application:
    name: payment-service
  datasource:
    url: ${PAYMENT_SERVICE_DATASOURCE_URL:jdbc:h2:mem:payment_service;MODE=PostgreSQL;DB_CLOSE_DELAY=-1}
    driver-class-name: ${PAYMENT_SERVICE_DATASOURCE_DRIVER:org.h2.Driver}
    username: ${PAYMENT_SERVICE_DATASOURCE_USERNAME:sa}
    password: ${PAYMENT_SERVICE_DATASOURCE_PASSWORD:}
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
  flyway:
    enabled: true
    locations: classpath:db/migration
    validate-on-migrate: true
  threads:
    virtual:
      enabled: true

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,prometheus
  endpoint:
    health:
      probes:
        enabled: true

logging:
  level:
    com.bank.platform.payment: INFO

bank:
  error:
    service-id: payment
    layer: L5
    boundary: INTERNAL
