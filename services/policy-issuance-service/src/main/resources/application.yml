server:
  port: 8099

spring:
  application:
    name: policy-issuance-service
  datasource:
    url: ${POLICY_ISSUANCE_SERVICE_DATASOURCE_URL:jdbc:h2:mem:policy_issuance_service;MODE=PostgreSQL;DB_CLOSE_DELAY=-1}
    driver-class-name: ${POLICY_ISSUANCE_SERVICE_DATASOURCE_DRIVER:org.h2.Driver}
    username: ${POLICY_ISSUANCE_SERVICE_DATASOURCE_USERNAME:sa}
    password: ${POLICY_ISSUANCE_SERVICE_DATASOURCE_PASSWORD:}
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
  flyway:
    enabled: true
    locations: classpath:db/migration
    validate-on-migrate: true
  threads:
    virtual:
      enabled: true

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,prometheus
  endpoint:
    health:
      probes:
        enabled: true

logging:
  level:
    com.bank.platform.policy: INFO

bank:
  error:
    service-id: policy
    layer: L5
    boundary: INTERNAL
