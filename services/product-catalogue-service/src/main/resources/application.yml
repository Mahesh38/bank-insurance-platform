server:
  port: 8094

spring:
  application:
    name: product-catalogue-service
  datasource:
    url: ${PRODUCT_CATALOGUE_SERVICE_DATASOURCE_URL:jdbc:h2:mem:product_catalogue_service;MODE=PostgreSQL;DB_CLOSE_DELAY=-1}
    driver-class-name: ${PRODUCT_CATALOGUE_SERVICE_DATASOURCE_DRIVER:org.h2.Driver}
    username: ${PRODUCT_CATALOGUE_SERVICE_DATASOURCE_USERNAME:sa}
    password: ${PRODUCT_CATALOGUE_SERVICE_DATASOURCE_PASSWORD:}
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
  flyway:
    enabled: true
    locations: classpath:db/migration
    validate-on-migrate: true
  threads:
    virtual:
      enabled: true

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,prometheus
  endpoint:
    health:
      probes:
        enabled: true

logging:
  level:
    com.bank.platform.catalogue: INFO

bank:
  error:
    service-id: catalogue
    layer: L5
    boundary: INTERNAL
