server:
  port: 8103

spring:
  application:
    name: reporting-mis-service
  threads:
    virtual:
      enabled: true

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,prometheus
  endpoint:
    health:
      probes:
        enabled: true

logging:
  level:
    com.bank.platform.reporting: INFO

bank:
  error:
    service-id: reporting
    layer: L5
    boundary: INTERNAL
