/**
 * Domain model, invariants and outbound ports for bounded context #{{CONTEXT_ID}} — {{SERVICE_NAME}}.
 *
 * <p>Domain stays pure Java — no Spring, Jackson or Jakarta persistence annotations.
 */
package {{PACKAGE}}.domain;
